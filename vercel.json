{
    "buildCommand": "cd apps/client && npm install && npm run build",
    "outputDirectory": "apps/client/dist",
    "installCommand": "npm install --ignore-scripts && npm install --workspaces",
    "framework": null
}